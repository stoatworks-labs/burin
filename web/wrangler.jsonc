// Rasterizer web demo — static-assets-only Worker.
// Deploy from web/: cf-run npx wrangler deploy  (see reference_cloudflare_access)
{
  "name": "rasterizer",
  "compatibility_date": "2026-08-17",
  "assets": {
    "directory": "./public"
  },
  "routes": [
    { "pattern": "rasterizer.stoatworks-labs.com", "custom_domain": true }
  ]
}
